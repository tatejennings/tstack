# Upkeep self-review — 2026-06-21

## 1. Run header

| Field | Value |
|---|---|
| Date | 2026-06-21 |
| Repo `version` | `0.5.0` (`.claude-plugin/plugin.json:3`) |
| Latest CHANGELOG heading | `## [0.5.0] — 2026-06-21` (in sync) |
| Claude Code docs host | `code.claude.com/docs/en/*` (old `docs.anthropic.com/en/docs/claude-code/*` now 301-redirects here) |
| Skills present | 11 — 7 chain (discover, product, architect, roadmap, plan, build, specify) + 1 on-ramp (ingest) + 3 off-chain (design, status, wrap) |

**Currency facts treated as authoritative this run:**
- **Claude model lineup** — Fable 5, Opus 4.8/4.7/4.6, Sonnet 4.6, Haiku 4.5 (`claude-haiku-4-5`). Sources: built-in `claude-api` skill + Anthropic models overview (`platform.claude.com/docs/en/about-claude/models/overview.md`, fetched 2026-06-21). "Haiku" remains a current, active tier name.
- **Claude Code hook schema** — `Stop` event with `stop_hook_active`, `transcript_path`, top-level `decision`/`reason`. Source: `code.claude.com/docs/en/hooks` (fetched 2026-06-21).
- **Tech-stack majors** — Xcode 26 (26.5, 2026-05-11, Wikipedia); Pydantic 2.x latest, no v3 (GitHub releases); Next.js 16, Tailwind 4, Postgres 18 (official sites).

## 2. Executive summary

| Severity | Count | Items |
|---|---|---|
| P0 (invariant/contract regression) | **0** | — |
| P1 (confirmed currency staleness) | **1** | Xcode "16+" → 26+ |
| P2 (sync-point drift) | **0** | all sync points agree |
| P3 (editorial/shape) | **0** | — |
| P4 (recommend-only / unconfirmed) | **3** | MCP/tool-access bullet; optional subagent mention; optional Postgres sample bump |
| Judgment (CHANGELOG scope) | **1** | repo-local `/upkeep` + `/ship` skills not in CHANGELOG — likely *correctly* absent |

**Auto-fixable sync mismatches: 0.** The repo is internally coherent — version, skill-count wording, and all three README enumerations (mermaid diagram, skill table, repo-layout tree) agree with `ls skills/`.

## 3. Group A findings — internal consistency (repo-only)

| Dim | Result | Notes |
|---|---|---|
| **A1 Skill-shape** | ✅ 11/11 conform | All skills follow frontmatter → Prereq check → Approach → Reference handoff → Handoff; all descriptions lead with what-it-does → "Use when…" → Input/Output → handoff. |
| **A2 Design invariants** | ✅ all 3 hold | (1) Verification external+quoted — `tstack-build/SKILL.md:34` mandates quoted command output; `tstack-roadmap/SKILL.md:130` refuses soft criteria at write time. (2) State on disk — plans at `docs/plans/{id}.md`; no `.tstack/state.json` reintroduced (BACKLOG confirms intentional removal). (3) Fresh session per milestone — `tstack-plan` and `tstack-build` both explicitly refuse auto-chaining. |
| **A3 Cross-skill contracts** | ✅ 7/7 pass | AGENTS.md owned solely by architect (`tstack-architect/SKILL.md:164-166`); plan at `docs/plans/{id}.md` (plan writes / build reads); ROADMAP one regenerator + one append-only editor with `Docs last synced:` marker + `(surgical: …)`; "Done when" testable at write time; ingest writes only `docs/_adopted/` with `## Open gaps`; four foundational ADRs + optional AI ADR, DECISIONS/TESTING mandatory at every size; M0 / i0 mandatory and refused-without. |
| **A4 Editorial conventions** | ✅ 3/3 pass | No acronym filenames (all "PRD" hits are *contrasts* showing PRODUCT.md is correct); product vs specify carry mutually-exclusive negative phrases; AI-feature criteria use eval-based format (eval set + quality bar + fallback), demonstrated in `tstack-product/references/example-output.md`. |
| **A5 Sync-point drift** | ✅ zero drift | version ↔ CHANGELOG match; skill-count wording agrees across plugin.json / marketplace.json / README / CLAUDE.md; README mermaid (11 nodes), skill table (11 rows), repo-layout tree (11 entries) all match `ls skills/`. |
| **A6 CHANGELOG discipline** | ⚠ 1 judgment item | `## [Unreleased]` present and empty. Commits `1acd15a` (`/upkeep`) and `cc52062` (`/ship`) added repo-local skills under `.claude/skills/` + edited CLAUDE.md, with no CHANGELOG entry. See judgment note below. |

## 4. Group B findings — external currency

| Item | Repo value (file:line) | Live current value | Source | Verdict | Recommendation |
|---|---|---|---|---|---|
| **B1** Claude model/tier refs | "Haiku" / "Claude (Haiku)" / "Haiku-class" — `tstack-architect/references/example-output.md:31,93,180`, `tstack-discover/references/example-output.md:97` | "Haiku" current tier (Haiku 4.5) | claude-api skill + platform.claude.com (2026-06-21) | **fresh** | None. Bare tier name (no version) is the right call for stable example docs — never goes stale while the tier exists. |
| **B2** Stop-hook jq paths | `stop_hook_active`, `transcript_path`, `decision`/`reason` — `README.md:105,106,113` | identical field names | code.claude.com/docs/en/hooks | **fresh** | None. README's own "field names can shift" caveat + `// default` fallbacks keep it resilient. |
| **B2** transcript entry field schema | `README.md:109-111` | per-entry schema not published in docs | code.claude.com/docs/en/hooks | **unconfirmed** | Leave as-is; README already hedges and points at `/hooks`. |
| **B2** docs host links | (repo-wide grep) | `code.claude.com` | observed redirect | **n/a** | No action — grep confirms **zero** `docs.anthropic.com` links in repo. |
| **B2** Tool/MCP access in ai-strategy | absent — `tstack-architect/SKILL.md:97-104` | MCP / function-calling is a real arch decision for agentic AI | code.claude.com docs | recommend-only | Consider adding a "Tool & data access (e.g. MCP servers, function calling)" bullet to the ai-strategy coverage list. Strongest of the feature suggestions. |
| **B2** Subagents in plan/build | absent across `skills/` | subagents are current | code.claude.com docs | recommend-only | Optional single sentence in `tstack-plan` for large milestones. Leave `tstack-build` serial+quoted to protect the verification invariant. |
| **B3** Xcode default | "Xcode 16+" — `tstack-architect/SKILL.md:141` | **Xcode 26** (26.5, 2026-05-11) | en.wikipedia.org/wiki/Xcode | **confirmed-stale** | Bump to "Xcode 26+". Apple moved to calendar-year versioning (16 → 26 late 2025); "16+" reads ~3 cycles behind. |
| **B3** Pydantic default | "Pydantic v2" — `tstack-architect/SKILL.md:143` | 2.x latest, no v3 | github.com/pydantic/pydantic/releases | **fresh** | None. |
| **B3** dated phrasing | "current as of early 2026", "2026 default/norm" — `SKILL.md:40,60,134,156` | accurate | — | **fresh** | None — these are the skill's own staleness disclaimers, working as intended. |
| **B3** Postgres in example | "Postgres 16 (Neon)" — `references/example-output.md:24` | Postgres 18 | postgresql.org | fresh (illustrative) | Optional sample bump to 18; it's example text, not an asserted default. Low priority. |

## 5. Auto-fix manifest

**None.** No A5 sync-point mismatches were found, so there are no safe deterministic edits to apply. Every candidate change this run is either a version/judgment call (recommend-only per policy) or a no-op.

## 6. Recommend-only list (judgment — not auto-applied)

1. **[P1] Bump Xcode default `16+` → `26+`** at `tstack-architect/SKILL.md:141`. Confirmed-stale by ~3 release cycles. Recommend-only because changing a pinned version is a judgment call (policy: B3 version changes never auto-applied). Carries a `## [Unreleased]` CHANGELOG entry if applied.
2. **[P4] Add a Tool/MCP-access bullet** to the ai-strategy coverage list (`tstack-architect/SKILL.md:97-104`) — "what external tools/data sources does the model reach, and via what protocol (MCP / function calling)". The strongest feature-reference suggestion; a real architecture gap for agentic AI products.
3. **[P4] Optional subagent mention** in `tstack-plan` for large-milestone parallel exploration. Deliberately *not* recommended for `tstack-build` — keeping verification serial and quoted protects the design invariant.
4. **[P4] Optional Postgres sample bump** `16 → 18` at `references/example-output.md:24`. Illustrative only; cosmetic currency.

## 7. Open questions

- **CHANGELOG scope for repo-local skills.** `/upkeep` and `/ship` live under `.claude/skills/` and are explicitly *never shipped* by the plugin loader (CLAUDE.md says so). The CHANGELOG header reads "All notable changes to **the TStack plugin**." On that wording these two skills are **correctly absent** — they're repo dev/maintenance tooling, not plugin behavior. The only reason to add entries is that both commits also touched CLAUDE.md (a tracked manifest). **My lean: no CHANGELOG entries required** — they aren't plugin-deliverable changes. Flagging for your call rather than auto-adding, since "does non-shipped tooling belong in the plugin CHANGELOG" is a scope decision, not a mechanical sync fix.
