# Upkeep review — 2026-06-23

## 1. Run header

- **Date:** 2026-06-23
- **Repo `version`:** 0.5.1 (`.claude-plugin/plugin.json`)
- **Latest CHANGELOG release heading:** `## [0.5.1] — 2026-06-21` (matches `version` ✓; an `## [Unreleased]` section sits above it with this session's entries)
- **Skills present (12):** tstack-architect, tstack-autopilot, tstack-build, tstack-design, tstack-discover, tstack-ingest, tstack-plan, tstack-product, tstack-roadmap, tstack-specify, tstack-status, tstack-wrap
- **Claude Code version observed:** not directly observable from the repo; hook schema cross-checked live (below)
- **Context:** Run immediately after a session that (a) added the new `tstack-autopilot` skill and (b) removed branch protection as a TStack-suggested/required item across `tstack-roadmap` + references, `tstack-build`, `CLAUDE.md`, and `README.md`. A primary goal of this run was to confirm those changes introduced no drift or regressions.

**Facts treated as current (each verified against a fetched source, not memory):**

| Fact | Value used | Source |
|---|---|---|
| Claude model families/tiers | Fable 5, Opus 4.8, Sonnet 4.6, Haiku 4.5 (`claude-haiku-4-5`, cheapest tier, $1/$5 MTok); Mythos 5 | `claude-api` skill + WebFetch `platform.claude.com/docs/en/about-claude/models/overview.md` |
| Claude Code hook schema | Stop input includes `stop_hook_active`, `transcript_path`; output uses `{decision, reason}` | WebFetch `code.claude.com/docs/en/hooks` (raw HTML cross-check) |
| Subagents / MCP | Current features; named agent types (`Explore`, `feature-dev:code-architect`) are real | `code.claude.com/docs/en/sub-agents` + session agent registry |
| Next.js | major 16 (16.2 stable) | nextjs.org/blog |
| Xcode | 26.5 stable (27 beta only) | developer.apple.com/news/releases |
| Pydantic | v2 (2.13.x; no v3) | github.com/pydantic/pydantic/releases |
| Tailwind | v4 (4.3.1) | github.com/tailwindlabs/tailwindcss/releases |
| Postgres | 18 stable (19 beta only) | postgresql.org |

## 2. Executive summary

**Clean run. No P0/P1/P2 findings. Zero drift introduced by this session's changes.**

| Severity | Count | Items |
|---|---|---|
| P0 — invariant/contract regression | 0 | — |
| P1 — confirmed currency staleness | 0 | — |
| P2 — sync-point drift | 0 | — |
| P3 — editorial/shape & enhancement | 2 | build "Hand-back" section name; build missing subagent note |
| P4 — unconfirmed currency | 0 | — |
| (examined, not a finding) | 1 | "PRD" in trigger phrases — false positive, rejected |

The new `tstack-autopilot` skill is correctly wired into **all five sync points** (version, count wording across 4 files, mermaid diagram, skill tables, repo tree) and conforms to its (justified) manual-only shape. The branch-protection removal is **consistent and complete** — M0 remains mandatory for CI/secrets/deploy/observability/lint/test-runner, and no location still requires branch protection. All Claude-ecosystem and tech-stack references are current.

## 3. Group A findings (internal consistency — repo-only)

| Dim | file:line | Expected | Found | Severity | Auto-fixable? |
|---|---|---|---|---|---|
| A1 | `skills/tstack-build/SKILL.md:133` | Section named `## Handoff` (canonical shape) | `## Hand-back` | P3 | Mechanical, but **recommend-only** (see below) |
| A2 | — | No invariant regressions | None. Autopilot's intra-run plan→build is sanctioned by CLAUDE.md (PR-and-stop, never chains to the *next* milestone); VERIFY keeps quoted-output discipline | — | — |
| A3 | — | All cross-skill contracts hold | All intact. Branch-protection removal verified coherent; M0 still mandatory; autopilot uses `docs/plans/{id}.md` + `{id}-report.md` | — | — |
| A4 | (product/discover/ingest) | Full-word **filenames** | "PRD" appears only in trigger phrases & references to the user's own docs — **not** a filename violation | n/a | No — false positive, rejected |
| A5 | — | All sync points enumerate the 12 present skills | All five in sync; autopilot present everywhere | — | — |
| A6 | `CHANGELOG.md` Unreleased | Both change-sets logged | Branch-protection removal (`### Changed`) + autopilot (`### Added`) both present and well-formed | — | — |

**A1 detail.** `tstack-build`'s terminal section is `## Hand-back` (content: "end the session cleanly — the next milestone is planned in a fresh session"). It breaks the literal `→ Handoff` symmetry CLAUDE.md asks to preserve, but the name is arguably *semantic*: build is the loop's terminal skill and hands back to the user/fresh-session rather than forward to a next skill. **Assessment: defensible as-is; low-confidence defect.** Rename to `## Handoff` only if strict symmetry is preferred. Not auto-applied (editorial judgment, per report-vs-fix policy).

**A4 detail (rejected).** The convention (`tstack-architect/SKILL.md:182`, CLAUDE.md § Editorial Conventions) is explicitly about **filenames** — "`PRODUCT.md`, not `PRD.md`." The flagged "PRD" instances are (a) user-facing trigger phrases like "let's write the PRD" (`tstack-product/SKILL.md:3`, `tstack-discover/SKILL.md:51`, `tstack-ingest/SKILL.md:89`) that capture how users actually speak and *improve* auto-triggering, and (b) descriptions of the user's own pre-existing artifact ("a written PRD…", `tstack-ingest/SKILL.md:3,8`). No TStack filename uses the acronym. Editing these would degrade trigger coverage. **No action.**

## 4. Group B findings (external currency — verified against fetched sources)

| Item | repo value (file:line) | current live value | source | verdict | recommendation |
|---|---|---|---|---|---|
| B1 — "Claude (Haiku)" slug ADR | `tstack-architect/references/example-output.md:31,93,180` | Haiku tier current (`claude-haiku-4-5`, cheapest) | platform.claude.com models overview | **fresh** | none — tier-name (version-agnostic) by design |
| B1 — discover cost line | `tstack-discover/references/example-output.md:97` ("Haiku-class", ~$0.04 sample) | Haiku-class current; figure is illustrative | same | **fresh** | none |
| B1 — architect AI-strategy guidance | `tstack-architect/SKILL.md` (model-agnostic) | n/a — names no specific model | same | **fresh** | none |
| B2 — README Stop-hook jq | `README.md:109,110,117` (`stop_hook_active`, `transcript_path`, `{decision,reason}`) | All three field paths match current schema | code.claude.com/docs/en/hooks | **fresh** | none |
| B2 — subagent notes (plan/architect/autopilot) | `tstack-plan/SKILL.md:57`, `tstack-architect/SKILL.md:101`, `tstack-autopilot/SKILL.md:39,47` | All v0.5.1 additions present & accurate; autopilot uses real agent types | sub-agents docs | **fresh** | none |
| B2 — `tstack-build` subagent note | (absent — grep finds none) | Subagents current; sibling `tstack-plan` and `tstack-autopilot` both use them | sub-agents docs | gap | **P3 recommend** — see §6 |
| B3 — Next.js / Xcode / Pydantic / Tailwind / Postgres | `tstack-architect/SKILL.md:141-145`, `references/example-output.md:21-24` | 16 / 26.5 / v2 / v4 / 18 — all latest stable | vendor release pages | **fresh** | none; v0.5.1's Xcode 16→26 & Postgres 16→18 bumps confirmed correct |

No confirmed-stale (P1) and no unconfirmed (P4) currency items.

## 5. Auto-fix manifest

**None.** All five A5 sync points (the auto-fixable class) are already in sync — there is nothing mechanical and one-right-answer to apply. The two P3 items are editorial/enhancement judgment calls (recommend-only), and the A4 candidate was rejected as a false positive.

## 6. Recommend-only list (judgment — not auto-applied)

1. **P3 — `tstack-build` lacks a subagent/parallel-explore note.** Its sibling `tstack-plan` gained one in v0.5.1 (`tstack-plan/SKILL.md:57`) and `tstack-autopilot` delegates broad reading to `Explore` subagents during execution (`tstack-autopilot/SKILL.md:39`), but interactive `tstack-build` never mentions subagents. For a milestone spanning several independent areas, build does the same broad code-surveying (finding reuse targets, tracing patterns) where subagents keep the loop's context lean. *Suggested*: add one sentence to build's implementation section, e.g. *"For a milestone touching several independent areas, delegate broad code-surveying to `Explore` subagents and implement from their summaries — keep the edits in this session."* Pair with a CHANGELOG Unreleased entry. (Severity only P3 — build's serial nature makes this less load-bearing than in plan.)

2. **P3 — `tstack-build` final section `## Hand-back`.** Rename to `## Handoff` only if strict cross-skill section symmetry is wanted; otherwise leave (the name is semantically apt for the loop's terminal skill). Low priority.

## 7. Open questions

- **Release timing.** The `## [Unreleased]` section now holds two substantive entries (autopilot + branch-protection removal). Adding a new skill + a framework-wide policy change is arguably a **minor** bump (0.6.0). Tagging is a human decision (`/ship`) — flagged, not actioned. No `version` bump made.
- **Future currency watch-items (not actionable now):** Next.js 17, Postgres 19, and Xcode 27 are all in beta/canary and will be the next stack bumps once stable.
